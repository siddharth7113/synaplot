"""Writes the LaTeX source for a diagram.

This package holds the code that turns a diagram into TikZ and wraps it in a
document, along with the TikZ style files it depends on. Compiling that source
into a PDF or an image happens elsewhere, in :mod:`synaplot.render`.
"""
